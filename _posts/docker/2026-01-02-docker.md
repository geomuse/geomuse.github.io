---
layout: post
title:  docker学习 docker pull
date:   2026-01-02 09:01:00 +0800
image: 13.jpg
tags: 
    - docker 
    - linux
---

# pull

从docker hub下载镜像

```
sudo docker pull ubuntu:latest
```

镜像名称中 / 为之前指定的使用者

# build

```
sudo docker build --tag  hello:0.1 .
```

用当前目录(.)的 Dockerfile 构建一个名为 hello、版本为 0.1 的 Docker 镜像

# run

```
sudo docker run --name hello-nginx -d -p 80:80 -v /root/data:/data hello:0.1
```